import { TabCustomizationScreen } from '@screens/settings/TabCustomizationScreen';

/** Full-screen editor for the customizable bottom bar (reached from "More"). */
export default function CustomizeTabsRoute() {
  return <TabCustomizationScreen />;
}
