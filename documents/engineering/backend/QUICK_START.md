# Quick Start: Deploy Image Extraction Feature

## What's Ready

✅ **Code Implementation Complete:**
- Image extraction functions added to [handler.py](../lambda-processor/handler.py)
- PyMuPDF and Pillow dependencies added to [requirements.txt](../lambda-processor/requirements.txt)
- Task drafts now populate `image_ids` field
- Images linked to findings and stored with AI descriptions

✅ **Deployment Scripts Ready:**
- All scripts organized in `backend/scripts/` folder
- CloudShell deployment package prepared
- Automated testing and verification scripts

## Deploy in 3 Steps

### Step 1: Start Deployment

```bash
cd /Users/andreitekhtelev/Desktop/SimpleHouseApp/backend/scripts
./ONE_COMMAND_DEPLOY.sh
```

This will:
- ✅ Prepare the deployment package
- ✅ Open AWS CloudShell in your browser
- ✅ Copy build commands to your clipboard

### Step 2: Build in CloudShell (1 Manual Step)

When CloudShell opens in your browser:

1. **Upload file:** Click "Actions" → "Upload file"
   - Select: `backend/lambda-processor/cloudshell-deploy.tar.gz`
   - Wait for upload to complete

2. **Paste commands:** Press `Cmd+V` (commands are already in clipboard)
   - The build will run automatically (~3 minutes)
   - Wait for "✅ Lambda deployed!" message

3. **Return to terminal:** Press ENTER in your terminal where the script is waiting

### Step 3: Automatic Testing

The script will automatically:
- ✅ Verify Lambda deployment
- ✅ Reprocess Electrical report.pdf with image extraction
- ✅ Display verification results showing:
  - Total images extracted (expecting 20+)
  - Sample images with AI descriptions
  - Task drafts with populated image_ids
  - Complete summary statistics

## What Gets Extracted

From the Electrical report.pdf:
- **20+ images** from the inspection report
- **AI descriptions** generated by Claude Vision
- **System categories** (electrical, plumbing, roof, etc.)
- **Automatic linking** to findings and task drafts
- **Thumbnails** generated for fast loading

## Verification

After deployment completes, you can manually verify:

```bash
# Check images extracted
npx wrangler d1 execute simple-house-db --env production --remote \
  --command "SELECT COUNT(*) as count FROM report_images WHERE report_id = '28fd554e-b641-45ea-97d3-afe8f0509854'"

# View sample images
./test-image-extraction.sh
```

## Time Estimate

- CloudShell build: ~3 minutes
- Lambda deployment: ~30 seconds
- Report processing: ~2-5 minutes
- **Total: ~6-9 minutes**

## Troubleshooting

**If CloudShell build fails:**
- Check that you uploaded the correct tar.gz file
- Verify you have Lambda update permissions
- Try pasting the commands again

**If no images extracted:**
- Check Lambda logs: `aws logs tail /aws/lambda/inspection-report-processor --follow --region us-east-1`
- Verify PyMuPDF installed correctly in build output
- Run `./reprocess-report.sh` to try again

## Alternative: Manual Deployment

If the one-command script doesn't work, you can deploy manually:

1. Open CloudShell: https://us-east-1.console.aws.amazon.com/cloudshell/home?region=us-east-1
2. Upload `cloudshell-deploy.tar.gz`
3. Run these commands:

```bash
tar -xzf cloudshell-deploy.tar.gz
mkdir -p package
pip3 install --target package/ -r requirements.txt
cp handler.py package/
cd package && zip -r ../lambda-function.zip . && cd ..
aws lambda update-function-code \
  --function-name inspection-report-processor \
  --zip-file fileb://lambda-function.zip \
  --region us-east-1
```

4. Then run from your local terminal:

```bash
cd /Users/andreitekhtelev/Desktop/SimpleHouseApp/backend/scripts
./reprocess-report.sh
./test-image-extraction.sh
```

## Next Steps

After successful deployment:
1. ✅ Images will appear in task draft details in the mobile app
2. ✅ Thumbnails will load quickly in task lists
3. ✅ Full-size images available on tap
4. ✅ AI descriptions help identify issues visually

---

**Ready to deploy?** Run `./ONE_COMMAND_DEPLOY.sh` to get started!
