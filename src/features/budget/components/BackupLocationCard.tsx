/**
 * Budget's spelling of the shared "your backup is here" card.
 *
 * The component moved to `@components/backup/BackupLocationCard` when House
 * needed the same breadcrumb + Open button. Its Budget wording is the default,
 * so this re-export renders exactly what it always did.
 */
export { BackupLocationCard } from '@components/backup/BackupLocationCard';
