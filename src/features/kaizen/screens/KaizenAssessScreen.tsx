/**
 * Kaizen "Assess" tab → the ported Symply Life Assess screen.
 * `KaizenAssessScreen` is the shell/tab contract; the implementation is the donor Assess port.
 */
export { AssessScreen as KaizenAssessScreen } from './AssessScreen';
