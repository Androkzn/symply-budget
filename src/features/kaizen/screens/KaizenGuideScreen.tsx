/**
 * Kaizen "Guide" tab (route `mira`) → the ported Symply Life AI coach.
 * `KaizenGuideScreen` is the shell/tab contract; the implementation is the donor CoachChat port.
 */
export { CoachChatScreen as KaizenGuideScreen } from './CoachChatScreen';
