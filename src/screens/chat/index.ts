// Household chat rooms (list / room / settings) moved to the shared module at
// `src/features/chat`. `ChatScreen` is the legacy AI report-Q&A surface (over
// `@api/chat`), unrelated to household chat, and stays here.
export { ChatScreen } from './ChatScreen';
